repos:
  # Уазываем для репосов в моем (belas80) гите
- id: github.com/belas80/devops-diplom-netology

  # На стороне клиентского конфига разрешите изменять workflow
  allowed_overrides: [workflow]
  allow_custom_workflows: true